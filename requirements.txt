# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0

# PDF extraction
PyMuPDF>=1.24.0

# Default LLM provider (free tier via https://aistudio.google.com/apikey)
google-generativeai>=0.8.0

# Optional providers — install whichever you set LLM_PROVIDER to:
#   pip install anthropic     # for LLM_PROVIDER=anthropic
#   pip install openai        # for LLM_PROVIDER=openai
# LLM_PROVIDER=mock needs no extra install at all.

# Dev / testing
pytest>=8.0.0
